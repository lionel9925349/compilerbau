// own test

type A = array[10] of int;
type my_int = int;

proc main(){
	var m1: int;
	var m2: A;
	var m3: my_int;
        printi(m1);
        m1 := 2 + (3 - 2);
	m2 := 2 + 2;

	m3 := 'C';
	m1 := 'C' + 12;
	b(3, 4);
	while(m2 > 0) { 
 		printi(m2);
 		m2 := m2 - 1;
		m3 := m2;
		
		while( m3 < 100) {
			printc(m3);
			m3 := m3 - 1;
			m1 := 2 + 3 - 1 * (3 - 2);
			printi(m1);
		}
	}
}

proc a(a1: int ) {
}

proc b(ref b1: int, b2: int, b3: int, ) {
}

